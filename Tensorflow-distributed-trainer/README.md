# TF Distributed Trainer Module
- Use this module to perform distributed training across multiple machines.
- Module usage : instantiate `TF_DistributedServer` and use it as shown in ifmain part of `trainer.py`
